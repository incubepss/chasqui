import { Routes } from '@angular/router';

import { MulticatalogListComponent } from './components/multicatalog-list/multicatalog-list.component';

export const routes: Routes = [
  {
    path: '',
    component: MulticatalogListComponent,
  },
];
